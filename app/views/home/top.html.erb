<h1>Home#top</h1>
<p>Find me in app/views/home/top.html.erb</p>
<div>
    <% if user_signed_in? %>

      <%= current_user.total %>

      <%= form_tag("/home/#{current_user.id}/add") do  %>
      <input type="text"  value="how much" name="resultPerformance">
      <input type="submit" value="送信">
      <% end %>

      <% @results.each do |result| %>
      <tr>
        <td><%= result.user.email %></td>
        <td><%= result.performance %></td>
      </tr>
      <br>
      <%end%>
      <%= line_chart @results.pluck(:created_at, :total), width: "800px", height: "500px" %>
    <% end %>
   

    <!-- <a href="input">input</a> -->
    <p>このサイトは<a href="https://twitter.com/ryouhei_0705">ryouhei0705</a>が運営しています。また、Amazonのアソシエイトとして、ryouhei0705は適格販売により収入を得ています。</p>
</div>